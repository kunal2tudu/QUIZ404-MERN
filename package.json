{
  "name": "quiz404-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist && cd ../server && npm install check-disk-space"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.0.2",
    "nodemailer": "^7.0.11"
  }
}